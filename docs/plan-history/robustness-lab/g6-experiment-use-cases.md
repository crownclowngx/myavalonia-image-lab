# G6 实验用例

新增基线准备、计划、串行运行和结果聚合窄用例。每个 Profile 先由正式协议嵌入，并由正式提取链完成未扰动回读；每个案例都从该不可变基线克隆，取消的未完成 trial 不提交。

结果包括分步观察、首次失败、失败后恢复、两类质量、16×16 局部网格、成功率和失败原因计数。尺寸不同时质量返回 `N/A/SizeMismatch`。Session 只长期保留结构化结果，不按案例保存完整图片，避免内存随案例数线性增长。
