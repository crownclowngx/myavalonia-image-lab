# G4 响应、差异与解释

- 日期：2026-08-30；状态：完成。
- 复用 `Fft2DTransform`，把核中心搬到 256² 周期网格原点，输出 fftshift 对数幅值、相位、DC、最大幅值和中心截面。
- 双核显示 `sqrt(|Hx|²+|Hy|²)` 摘要；固定说明不含偏置、边界、裁切与空间 Magnitude 相位。
- 新增 RGB 绝对差异、有符号亮度差异、MAE/RMSE/最大差异/变化像素和完整贡献 DTO。
- 测试覆盖 Identity 全通、低通 DC=1、导数 DC=0、bias 不变、divisor 缩放、零差异中性灰及贡献求和。
