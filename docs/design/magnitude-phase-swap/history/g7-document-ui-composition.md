# G7 Document、UI、组合与 Standalone

状态：完成。

新增稳定 ID `myavalonia.plugin.image.lab.document.magnitude-phase-swap`，作为第二十个 Persistable Document 追加，继续零 Tool。View 提供双输入、三档画布、十个合法预设、插值、规范输入、A/B/Result 幅度与相位、重建、指标、探针和导出；Document 只协调用例、状态、generation 与 Bitmap。真实 Module、DI、编译绑定 Headless View、坐标映射、Scope 隔离、架构扫描及 Standalone 使用同一对象图。
