# G0 产品、数学与文档基线

日期：2026-09-02。

## 本阶段实际完成

- 冻结 V1 为灰度、方形规范分析画布，不宣称原尺寸或彩色处理；
- 冻结 256/512/1024、FitContain、白底 Alpha 合成、BT.601 亮度和显示内容矩形；
- 冻结两种交换、A/B 单分量、幅度线性插值和相位最短圆弧插值；
- 冻结零幅度相位、共轭代表、自共轭频点、IFFT 虚部和投影语义；
- 冻结供体频域误差、空间 NCC/梯度相关、条件性 PSNR-Y/SSIM-Y 和裁切诊断；
- 冻结 SOLID 分层、朴素模式、中文详细注释、双频谱资源上限和 generation 提交；
- 建立专用 README、实施、数学、测试、指南、用户手册、Recipe、Report 和历史入口；
- 同步公共文档索引与未来能力状态。

## 未完成

- 没有新增或修改生产代码、测试代码、Module、DI、View 或 Standalone；
- 没有执行 restore、build、test、真实窗口或性能测量；
- 没有创建 G1–G9 完成记录；
- 没有使用 AIFLOW，没有新增 Windows CI，也没有执行发布门禁。

## 下一阶段入口条件

G1 开始前应复核现有 `ImageAnalysisProxyProjector`、`ImageChannelConverter`、`FrequencySpectrumBuilder` 和完整质量指标的实际复用边界，以独立公式 oracle 先建立规范画布测试。若现有白底亮度语义与本文不一致，必须先更新本文并记录偏差，不能让实现形成第二套隐含协议。

## 回滚

本阶段只有文档；回滚时删除本专用目录并恢复索引/未来能力链接即可，不影响现有十九个 Document 和任何持久数据。
