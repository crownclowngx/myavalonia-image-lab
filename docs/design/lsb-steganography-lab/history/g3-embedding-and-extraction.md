# G3 写入与提取

- 实际修改：新增不原地修改输入的写入器、复用同一位置算法的提取器、变化/通道/16×16 网格事实和 Frame BER。
- 自动证据：四通道×bit0/1×顺序/伪随机端到端，源缓冲不变、Alpha/跳过字节不变、±1/±2 边界和参数错配结构化失败。
- 设计取舍：取消通过异常退出，不返回半结果；Payload CRC 失败不暴露成功载荷。
- 遗留：完整像素探针的 UI 手势可在后续小版本增强，当前精确位置和变化由代理与等价数值提供。
- 回滚：删除独立 Engine，不触碰 FrequencyWatermarkCarrier。
