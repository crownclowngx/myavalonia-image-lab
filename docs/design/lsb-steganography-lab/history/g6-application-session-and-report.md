# G6 应用、Session 与报告

- 实际修改：新增准备、容量、写入分析、提取、脆弱性、PNG 导出、报告导出和二进制读取窄用例；Session 负责 Frame/图片所有权；JSON/CSV schema 1。
- 自动证据：内存与 PNG 双重自检、原子端口、报告隐私、非有限值、二进制读取与 Session 释放。
- 设计取舍：应用层协调但不实现像素公式或 JSON 拼接；二进制文件读取前后双门禁。
- 遗留：报告 schema 只含当前摘要字段，未来新增字段需升版。
- 回滚：先取消 DI 注册和报告适配，再删除应用目录。
