# G5 Document 生命周期

状态：完成。新增稳定 ID `myavalonia.plugin.image.lab.document.image-fingerprint`，成为第六个多实例 Persistable Document。Document 管理比较/稳定性两类取消、generation、Revision、路径失效、Session 和三张 Bitmap；快照只含路径和轻量参数，恢复不自动读图。迟到结果、关闭取消和 Scope 隔离均有自动门禁。
