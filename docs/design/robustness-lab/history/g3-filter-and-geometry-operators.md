# G3 滤波与几何算子

实现可分离高斯模糊、3×3/5×5 中值、固定 σ=1 的 unsharp mask、双线性缩放、裁剪、补边、固定画布平移/旋转和四角轻度透视。

滤波边界为 clamp-to-edge；缩放尺寸使用 AwayFromZero；旋转/透视采用逆向映射和未预乘 RGBA 双线性采样，越界使用显式填充色。透视矩阵用带主元的 8×8 消元求解并拒绝奇异矩阵。不会自动对齐、扩画布或恢复尺寸。
