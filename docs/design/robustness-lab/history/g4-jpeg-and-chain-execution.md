# G4 JPEG 与链执行

JPEG Strategy 位于 Infrastructure，通过正式 `IImageCodec` 完成内存编码—解码；非完全不透明输入可见阻断，不写中间文件。链执行器只查找显式登记的 Strategy、按列表顺序调用、跳过禁用步骤并触发前缀回调。

组合根逐个登记 18 个 Kind，测试同时验证重复拒绝和种类完整。链测试用非交换颜色操作证明顺序，并验证首次失败和失败后恢复语义。既有真实 PNG/JPEG 回读测试继续作为正式 codec 回归。
