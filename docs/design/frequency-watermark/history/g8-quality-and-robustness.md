# G8 质量、鲁棒性、性能与安全记录

状态：开发门禁完成（2026-08-30）

## 自动矩阵

- Stealth/Balanced/Robust：512×512 纹理图经像素量化后完整恢复。
- 正式字节：Balanced PNG 输出完整恢复；Robust JPEG 100 输出自检通过。
- 攻击：Robust 输出经 JPEG 95 重编码恢复；RGB 每通道确定性 ±1 噪声恢复。
- 纠错：单块 16 个符号错误恢复并报告；17 个错误拒绝。
- 误报：确定性无水印纹理返回 `NoSupportedWatermark`。
- 质量：写入返回 PSNR/全局 SSIM；PNG 用例要求 PSNR > 20 dB，JPEG 编解码要求 PSNR > 20 dB、SSIM > 0.70。
- 资源：64 MiB 编码文件读取前拒绝、16M 像素、1024 最大边分析预览和 16 MiB Payload 上限、Document 取消与 Scope 隔离。

复现命令见 [测试文档](../testing.md)。当前完整 Debug 结果为 44/44。

## 结论边界

这些数据冻结“开发回归能力”，没有冻结面向外部的照片恢复率。源图是确定性纹理，虽然输出确实经过 Avalonia/Skia PNG/JPEG 编码并以实际字节回读，但不能替代授权照片、插画、渐变与多编码器语料。

用户明确要求当前不使用 Windows CI 与发布门禁，因此正式照片语料、发布设备耗时/峰值内存、长时间泄漏和平台矩阵不在本轮伪装完成。发布时必须补充逐样本原始结果；不达标 Profile 应降级或删除。
