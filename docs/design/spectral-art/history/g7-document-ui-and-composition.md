# G7：Document、UI 与组合

新增专用 Document/View/区域控件。控件复用 UniformImageCoordinateMapper，支持 letterbox、手柄、Pointer capture、方向键和共轭显示；数值门禁仍在 Domain。稳定 ID 只追加在 Poisson 后，Module 为 18 个 Persistable Document、0 Tool；Standalone 通过真实 Module/DI 创建独立 Scope。
